
const AllAgents = () => {
  return (
    <div>
      {/* 
        same for clients


       1 - create custom hoook (action file) (if not created)
      
       2 - write all the functions for calling api 
          2.1 create - const [list, setList] = useState([])   
          2.1 - const getAllAgents = () => {} >>>> API ${API}/users/all-agents >>>> update the state setList
       3 - call getAllAgents in useEffect with empty deps
       4 - create function to get user detail ->  
          ${API}/users/${id}
      
       5 - display content in this in tables ()
          - link user detail -> call step 4 
      
      */}
    </div>
  )
}

export default AllAgents